#include <iostream>
#include <cstdio>
#include <cstring>
#include <cmath>
int jy[10000][10000];
int yin[10000];
int shu[10000];
int ys[100000];
int main(){
    int a;
    int yao;
    cin>>a;
    cin>>yao;
    for(int i=1;i<=a;i++){
        cin>>shu[i];
        cin>>yin[i];
        cin>>yds[i];
    }
    for(int i=1;i<=a;i++){
        for(int j=1;j<=yao;j++){
            if(j>=yds[x]){
                {
                    dp[i][j]=max(dp[i-1][j-[]])
                }
            }
        }
    }
}